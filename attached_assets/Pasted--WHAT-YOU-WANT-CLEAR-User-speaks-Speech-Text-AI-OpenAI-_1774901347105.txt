🧠 WHAT YOU WANT (CLEAR)

🎤 User speaks →
📝 Speech → Text →
🤖 AI (OpenAI/OpenRouter) →
🔊 Text → Voice (ElevenLabs) →
🎧 User hears response

👉 This is full voice AI assistant

⚠️ IMPORTANT REALITY

You need 2 parts:

🗣️ 1. Speech → Text

Use:

Browser API (SpeechRecognition) ✅ easiest
🔊 2. Text → Speech

Use:

ElevenLabs API ✅ high-quality voice
🧱 SYSTEM FLOW (FINAL ARCHITECTURE)
Frontend (React)
   ↓
SpeechRecognition (voice → text)
   ↓
Backend (/ai/chat)
   ↓
AI response
   ↓
Backend (/ai/tts - ElevenLabs)
   ↓
Audio file
   ↓
Frontend plays audio 🔊
🎤 1. SPEECH → TEXT (Frontend)
const startListening = () => {
  const recognition =
    new (window.SpeechRecognition || window.webkitSpeechRecognition)();

  recognition.lang = "en-IN"; // or hi-IN

  recognition.onresult = (event) => {
    const text = event.results[0][0].transcript;
    setInput(text);
    sendToAI(text); // auto send
  };

  recognition.start();
};
🤖 2. AI RESPONSE (You already have)

Use your /api/ai/chat

🔊 3. ELEVENLABS TEXT → SPEECH (Backend)
Install:
npm install axios
Backend Route:
import axios from "axios";

app.post("/api/ai/tts", async (req, res) => {
  try {
    const { text } = req.body;

    const response = await axios({
      method: "POST",
      url: "https://api.elevenlabs.io/v1/text-to-speech/YOUR_VOICE_ID",
      headers: {
        "xi-api-key": process.env.ELEVENLABS_API_KEY,
        "Content-Type": "application/json",
      },
      data: {
        text: text,
        model_id: "eleven_multilingual_v2",
      },
      responseType: "arraybuffer",
    });

    res.setHeader("Content-Type", "audio/mpeg");
    res.send(response.data);
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
});
🔊 4. PLAY AUDIO (Frontend)
const playVoice = async (text) => {
  const res = await fetch("/api/ai/tts", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ text }),
  });

  const blob = await res.blob();
  const audio = new Audio(URL.createObjectURL(blob));
  audio.play();
};
🌍 5. HINDI + ENGLISH SUPPORT
Detect language:
function detectLang(text) {
  return /[\u0900-\u097F]/.test(text) ? "hi" : "en";
}
ElevenLabs supports multilingual:

Use:

model_id: "eleven_multilingual_v2"
🎛️ 6. UI (LIKE CHATGPT VOICE MODE)

Add:

🎤 Mic button
🔊 Speaker toggle
Chat bubble